

public class Validator {
	
	public void validateRequest(Request request) {
		CarType carType = car.getType();
		int count = (Arrays.asList(CarColor.RED.name(), CarColor.GREEN.name(), CarColor.BLUE.name() || carType.equals(CarType.eqauls(CarType.PASSENGER) ? 0 : car.getPassengerCount();
	}

